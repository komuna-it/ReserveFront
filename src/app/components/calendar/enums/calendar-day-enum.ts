import { inject } from "@angular/core";
import { TranslocoService } from "@jsverse/transloco";
export enum CalendarDayEnum  {

    readonly loco = inject(TranslocoService);

0 = loco.translate('CALENDAR.MONDAY');
}